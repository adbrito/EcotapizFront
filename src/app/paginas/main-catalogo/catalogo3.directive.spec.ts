import { Catalogo3Directive } from './catalogo3.directive';

describe('Catalogo3Directive', () => {
  it('should create an instance', () => {
    const directive = new Catalogo3Directive();
    expect(directive).toBeTruthy();
  });
});

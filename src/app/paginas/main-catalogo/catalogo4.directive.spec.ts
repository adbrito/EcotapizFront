import { Catalogo4Directive } from './catalogo4.directive';

describe('Catalogo4Directive', () => {
  it('should create an instance', () => {
    const directive = new Catalogo4Directive();
    expect(directive).toBeTruthy();
  });
});

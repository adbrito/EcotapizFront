import { Catalogo2Directive } from './catalogo2.directive';

describe('Catalogo2Directive', () => {
  it('should create an instance', () => {
    const directive = new Catalogo2Directive(null);
    expect(directive).toBeTruthy();
  });
});

import { CatalogoDirective } from './catalogo.directive';

describe('CatalogoDirective', () => {
  it('should create an instance', () => {
    const directive = new CatalogoDirective();
    expect(directive).toBeTruthy();
  });
});

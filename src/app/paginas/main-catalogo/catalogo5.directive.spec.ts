import { Catalogo5Directive } from './catalogo5.directive';

describe('Catalogo5Directive', () => {
  it('should create an instance', () => {
    const directive = new Catalogo5Directive();
    expect(directive).toBeTruthy();
  });
});

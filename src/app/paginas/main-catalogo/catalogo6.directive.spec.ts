import { Catalogo6Directive } from './catalogo6.directive';

describe('Catalogo6Directive', () => {
  it('should create an instance', () => {
    const directive = new Catalogo6Directive();
    expect(directive).toBeTruthy();
  });
});

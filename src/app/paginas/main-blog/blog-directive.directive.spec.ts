import { BlogDirectiveDirective } from './blog-directive.directive';

describe('BlogDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BlogDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
